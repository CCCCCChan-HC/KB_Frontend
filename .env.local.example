# 本地开发环境配置示例
# 复制此文件为 .env.local 并填入实际值

# ===========================================
# 应用配置 (可选，用于 Dify 集成)
# ===========================================
NEXT_PUBLIC_APP_ID=your-dify-app-id
NEXT_PUBLIC_APP_KEY=your-dify-app-key
NEXT_PUBLIC_API_URL=https://api.dify.ai/v1

# ===========================================
# CAS 认证配置 (必需)
# ===========================================
# 客户端 CAS 配置 - 浏览器访问使用
NEXT_PUBLIC_CAS_BASE_URL=https://your-cas-server.com/cas
NEXT_PUBLIC_CAS_SERVICE_URL=https://your-domain.com/login

# 服务器端 CAS 配置 - 后端验证使用
# 注意：在 Docker 环境中，这里可能需要使用容器内网络地址
CAS_BASE_URL=https://your-cas-server.com/cas
CAS_SERVICE_URL=https://your-domain.com/login

# ===========================================
# NextAuth 配置 (必需)
# ===========================================
# 应用的完整 URL，必须与 CAS service URL 的域名一致
NEXTAUTH_URL=https://your-domain.com

# NextAuth 密钥，用于 JWT 签名和加密
# 生产环境中必须使用强密码（至少32字符）
# 可以使用以下命令生成：openssl rand -base64 32
NEXTAUTH_SECRET=your-very-secure-secret-key-at-least-32-characters-long

# ===========================================
# 环境设置
# ===========================================
NODE_ENV=development

# ===========================================
# HTTPS 配置 (可选)
# ===========================================
# 是否启用 HTTPS（Docker 部署时可用）
HTTPS=false

# SSL 证书域名（用于自签证书）
SSL_DOMAIN=localhost

# 自定义证书路径（可选）
# SSL_CERT_FILE=/path/to/your/certificate.crt
# SSL_KEY_FILE=/path/to/your/private.key

# ===========================================
# 配置说明
# ===========================================
# 1. CAS_BASE_URL 和 NEXT_PUBLIC_CAS_BASE_URL:
#    - 通常应该相同
#    - 在 Docker 环境中，服务器端可能需要使用内部网络地址
#
# 2. CAS_SERVICE_URL 和 NEXT_PUBLIC_CAS_SERVICE_URL:
#    - 必须完全相同
#    - 必须是 CAS 服务器能够访问的地址
#    - 必须与 NEXTAUTH_URL 的域名一致
#
# 3. NEXTAUTH_SECRET:
#    - 生产环境中必须更改默认值
#    - 建议使用至少 32 字符的随机字符串
#
# 4. 域名一致性:
#    - NEXTAUTH_URL、CAS_SERVICE_URL 的域名必须一致
#    - 确保 CAS 服务器配置中允许该域名作为 service